{%- from "govuk/components/inset-text/macro.njk" import govukInsetText -%}
<div class="app-log-card {{- ' ' + params.classes if params.classes }}">

  {%- if params.application %}
    <div class="app-log-card__note">
      <p class="govuk-body govuk-!-margin-bottom-0">{{ params.application.provider }}</p>
      <p class="govuk-body govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ params.application.course }} at {{ params.application.location }}</p>
      <p class="govuk-body-s">Ratified by {{ params.application.accreditedBody }}</p>

      {%- if params.application.rejectedReasons %}
        {%- set rejectedReasons = params.application.rejectedReasons -%}
        {% include "_includes/candidate-feedback.njk" %}
      {% endif -%}

      <p class="govuk-body govuk-!-margin-bottom-0">
        <a class="govuk-link" href="/application/{{ params.id }}">View application</a>
      </p>
    </div>
  {% endif -%}

  {%- if params.offer %}
    <div class="app-log-card__note">
      <p class="govuk-body govuk-!-margin-bottom-0">{{ params.offer.provider }}</p>
      <p class="govuk-body govuk-!-font-weight-bold govuk-!-margin-bottom-1">{{ params.offer.course }} at {{ params.offer.location }}</p>
      <p class="govuk-body-s">Ratified by {{ params.offer.accreditedBody }}</p>
      <p class="govuk-body govuk-!-margin-bottom-0">
        <a class="govuk-link" href="/application/{{ params.id }}/offer">View offer</a>
      </p>
    </div>
  {% endif -%}

  {%- if params.note %}
    <div class="app-log-card__note">
      <p class="govuk-body govuk-!-margin-bottom-1 govuk-!-font-weight-bold">{{ params.note.subject }}</p>
      <p class="govuk-body">{{ params.note.body }}</p>
      <p class="govuk-body govuk-!-margin-bottom-0">
        <a class="govuk-link" href="/application/{{ params.id }}/notes">View note</a>
      </p>
    </div>
  {% endif -%}

</div>
