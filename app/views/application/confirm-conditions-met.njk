{% extends "_layout.njk" %}

{% set application = data.applications[applicationId] %}
{% set name = [application["personal-details"]["given-name"], application["personal-details"]["family-name"]] | join(" ") %}

{% set title = "Check and confirm conditions have been met" %}

{% block pageNavigation %}
  {{ govukBackLink({
    href: "/application/" + application.id + '/mark-conditions'
  }) }}
{% endblock %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <h1 class="govuk-heading-xl">{{title}}</h1>

      {% include "_includes/application-summary.njk" %}

      <h2 class="govuk-heading-l">Conditions</h2>

      <ul class="govuk-list govuk-list--bullet govuk-!-margin-bottom-8">
        {% for condition in conditions %}
          <li>{{condition.description}}</li>
        {% endfor %}
      </ul>

      <form method="post">
        {{ govukButton({
          text: "Confirm conditions as met"
        }) }}
      </form>
      <p class="govuk-body"><a href="/application/{{ application.id }}">Cancel</a></p>
    </div>
  </div>

{% endblock %}
