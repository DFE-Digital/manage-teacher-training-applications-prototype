{% extends "_layout.njk" %}

{% set name = application.personalDetails.name %}

{% set title = "Check and confirm changes to offer" %}

{% block pageNavigation %}
  {{ govukBackLink({
    href: "/application/" + application.id + '/offer/change-provider/location'
  }) }}
{% endblock %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <span class="govuk-caption-l">{{name}}</span>
      <h1 class="govuk-heading-l">{{title}}</h1>

      <h2 class="govuk-heading-m">Your existing offer</h2>

     {% include "_includes/offer-summary.njk" %}

      <h2 class="govuk-heading-m">Your new offer</h2>

      {{ govukSummaryList({
        rows: [{
          key: {
            text: "Provider"
          },
          value: {
            html: data.trainingprovider
          },
          actions: {
            items: [
              {
                href: "/application/" + application.id + "/offer/change-provider",
                text: "Change",
                visuallyHiddenText: "provider"
              }
            ]
          }
        }, {
          key: {
            text: "Course"
          },
          value: {
            html: data.course
          },
          actions: {
            items: [
              {
                href: "/application/" + application.id + "/offer/change-provider/course",
                text: "Change",
                visuallyHiddenText: "course"
              }
            ]
          }
        }, {
          key: {
            text: "Location"
          },
          value: {
            html: data.location
          },
          actions: {
            items: [
              {
                href: "/application/" + application.id + "/offer/change-provider/location",
                text: "Change",
                visuallyHiddenText: "location"
              }
            ]
          }
        }
      ]}) }}

      {{ govukWarningText({
        text: "By making this offer, you guarantee a place on this course as long as the candidates meets the conditions. We’ll email the candidate with the details of this offer."
      }) }}

      <form method="post">
        {{ govukButton({
          text: "Confirm new offer"
        }) }}
      </form>
      <p class="govuk-body"><a href="/application/{{ application.id }}/offer">Cancel</a></p>
    </div>
  </div>

{% endblock %}
