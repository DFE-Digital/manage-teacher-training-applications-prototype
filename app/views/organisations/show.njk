{% extends "_layout.njk" %}

{% set primaryNavId = 'organisations' %}
{% set title = data.organisations[0].name %}

{% block content %}

  {{ appBanner({
    html: "<h2 class=\"govuk-heading-m\">"+flashMessage+"</h2>",
    type: "success",
    icon: false
  }) if flashMessage }}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <h1 class="govuk-heading-xl">{{title}}</h1>

      {% set tick %}
        {{ appIcon({
          classes: "govuk-!-margin-right-1",
          icon: "tick",
          hidden: true
        }) }}
      {% endset %}

      <h2 class="govuk-heading-m">
      For courses ratified by {{ data.organisations[0].name }} and run by {{ data.organisations[1].name }}
      </h2>

      {% set bannerHtml %}
        <p>{{ data.organisations[1].name }} have not set up permissions yet – only they can set up permissions. Contact them to do this.</p>
        <p>Staff at {{ data.organisations[0].name}} can still view applications in the mean time.</p>
      {% endset %}
      {{appBanner({
        html: bannerHtml
      })}}

      {# {{appBanner({
        text: "Contact " + data.organisations[1].name + " to change permissions.",
        classes: "govuk-!-margin-bottom-4"
      })}}

      <p>The following organisation(s) can make decisions:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[0].name }}</li>
      </ul>

      <p>The following organisation(s) can see safeguarding information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[1].name }}</li>
      </ul>

      <p>The following organisation(s) can see diversity information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[1].name }}</li>
      </ul>

       #}

      <h2 class="govuk-heading-m">
      For courses ratified by {{ data.organisations[0].name }} and run by {{ data.organisations[2].name }}
      </h2>

      {{appBanner({
        text: "Contact " + data.organisations[2].name + " to change permissions.",
        classes: "govuk-!-margin-bottom-4"
      })}}

      <p>The following organisation(s) can make decisions:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[0].name }}</li>
      </ul>

      <p>The following organisation(s) can see safeguarding information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[2].name }}</li>
      </ul>

      <p>The following organisation(s) can see diversity information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[2].name }}</li>
      </ul>

      <h2 class="govuk-heading-m">
      For courses ratified by {{ data.organisations[0].name }} and run by {{ data.organisations[3].name }}
      </h2>

      {{appBanner({
        text: "Contact " + data.organisations[3].name + " to change permissions.",
        classes: "govuk-!-margin-bottom-4"
      })}}

      <p>The following organisation(s) can make decisions:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[0].name }}</li>
      </ul>

      <p>The following organisation(s) can see safeguarding information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[3].name }}</li>
      </ul>

      <p>The following organisation(s) can see diversity information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[3].name }}</li>
      </ul>

      <h2 class="govuk-heading-m">
      For courses ratified by {{ data.organisations[0].name }} and run by {{ data.organisations[4].name }}
      </h2>

      <p>The following organisation(s) can make decisions:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[0].name }}</li>
      </ul>

      <p>The following organisation(s) can see safeguarding information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[4].name }} </li>
      </ul>

      <p>The following organisation(s) can see diversity information:</p>

      <ul class="govuk-list">
        <li>{{tick | safe }} {{ data.organisations[4].name }} </li>
      </ul>

      <p>You can also <a href="/organisations/show3">manage permissions for {{ data.organisations[4].name }}</a>.</p>

    </div>
  </div>

{% endblock %}
