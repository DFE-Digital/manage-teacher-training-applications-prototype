{% extends "_content-wide.njk" %}

{% set title = "Set up permissions between your own organisations and your partner organisations" %}

{% block primary %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">
      <p>You can control which organisations can see sensitive information, and those who can make decisions about applications.</p>

      <p>These permissions govern what individual users are able to do within their own organisations.</p>

      <p>For example, if your organisation can make decisions and your partner organisation cannot make decisions, then their users will not be able to make decisions on those particular applications.</p>

      <p>You need to set up permissions for the following partnerships:</p>

      <table class="govuk-table">
        <thead class="govuk-table__head">
          <tr class="govuk-table__row">
            <th scope="col" class="govuk-table__header">Your organisations</th>
            <th scope="col" class="govuk-table__header">Partner organisations</th>
          </tr>
        </thead>
        <tbody class="govuk-table__body">
         {% for item in items %}
          {% if not item.org.isAccreditedBody %}
            <tr class="govuk-table__row">
              <th scope="row" class="govuk-table__cell">{{ item.org.name }}</th>
              <td class="govuk-table__cell"><ul class="govuk-list govuk-list--bullet govuk-!-margin-bottom-0">
                  {% for p in item.partners %}
                    <li>{{ p.name }}</li>
                  {% endfor %}
                </ul>
              </td>
            </tr>
          {% endif %}
        {% endfor %}
      </tbody>
      </table>

      <form novalidate action="/onboard/information">
        {{govukButton({
          text: "Continue"
        })}}
      </form>
    </div>
  </div>

{% endblock %}
