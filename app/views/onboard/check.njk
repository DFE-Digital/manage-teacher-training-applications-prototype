{% extends "_layout.njk" %}

{% block beforeContent %}
  {{ govukBackLink({
    href: "/onboard/step3"
  }) }}
{% endblock %}

{% block content %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds">

      <span class="govuk-caption-xl">Set up permissions</span>
      <h1 class="govuk-heading-xl">Check permissions</h1>

      <form novalidate action="/onboard/confirmation">

        {# <h2 class="govuk-heading-m">
          For courses run and ratified by Somerset SCITT Consortium
        </h2>

        <p>Somerset SCITT Consortium can:</p>
        <ul class="govuk-list">
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }}  make decisions</li>
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} see safeguarding information</li>
        </ul>

        <p><a href="/onboard/step2">Change</a></p> #}

        <h2 class="govuk-heading-m">
          For courses run by Ventrus Teaching School Alliance and ratified by Somerset SCITT Consortium
        </h2>
        <p>Ventrus Teaching School Alliance can:</p>
        <ul class="govuk-list">
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} view all applications to these courses</li>
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} make decisions</li>
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} access safeguarding information</li>
        </ul>
        <p>Somerset SCITT Consortium can:</p>
        <ul class="govuk-list">
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} view all applications to these courses</li>
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} make decisions</li>
          <li>{{ appIcon({
              classes: "govuk-!-margin-right-1",
              icon: "tick",
              hidden: true
            }) }} access safeguarding information</li>
        </ul>

        <p class="govuk-!-margin-bottom-8"><a href="/onboard/step3">Change</a></p>

        {{govukButton({
          text: "Save permissions"
        })}}
      </form>
    </div>
  </div>

{% endblock %}
