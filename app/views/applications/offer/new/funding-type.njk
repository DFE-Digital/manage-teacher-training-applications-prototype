{% extends "_layout.njk" %}

{% set primaryNavId = "applications" %}
{% set caption = application.personalDetails.name %}
{% set title = "Select full time or part time" %}

{% block pageNavigation %}
{% if query.referrer == "location" %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "/offer/new/location"
  }) }}
{% else %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "offer/new/check"
  }) }}
{% endif %}
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    {% set headingHtml %}
      <span class="govuk-caption-l">{{ caption }}</span>
      {{ title }}
    {% endset %}

    <form action="/applications/{{ application.id }}/offer/new/funding-type" method="post" accept-charset="UTF-8" novalidate>

      {{ govukRadios({
        idPrefix: "edit-fundingType",
        name: "[new-offer][fundingType]",
        fieldset: {
          legend: {
            html: headingHtml,
            isPageHeading: true,
            classes: "govuk-fieldset__legend--l"
          }
        },
        items: [{
          value: "Fee paying",
          text: "Fee paying",
          checked: checked("['new-offer']['fundingType']", "Fee paying")
        }, {
          value: "Salaried",
          text: "Salaried",
          checked: checked("['new-offer']['fundingType']", "Salaried")
        }]
      }) }}

      {{ govukButton({
        text: "Continue"
      }) }}

    </form>

    <p class="govuk-body">
      <a class="govuk-link" href="/applications/{{ application.id }}/offer/new/course/cancel">Cancel</a>
    </p>

  </div>
</div>
{% endblock %}
