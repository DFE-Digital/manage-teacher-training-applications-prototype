{% extends "_layout.njk" %}

{% set primaryNavId = "applications" %}
{% set caption = application.personalDetails.name %}
{% set title = "Location" %}

{% block pageNavigation %}
{% if query.referrer == "study-mode" %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "/offer/edit/study-mode"
  }) }}
{% elif query.referrer == "offer" %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "/offer"
  }) }}
{% elif query.referrer == "check" %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "/offer/edit/check"
  }) }}
{% else %}
  {{ govukBackLink({
    text: "Back",
    href: "/applications/" + application.id + "offer/edit/check"
  }) }}
{% endif %}
{% endblock %}

{% block content %}
<div class="govuk-grid-row">
  <div class="govuk-grid-column-two-thirds">

    {% set headingHtml %}
      <span class="govuk-caption-l">{{ caption }}</span>
      {{ title }}
    {% endset %}

    <form action="{{ actions.save }}" method="post" accept-charset="UTF-8" novalidate>

      {{ govukRadios({
        idPrefix: "edit-location",
        name: "[edit-offer][location]",
        fieldset: {
          legend: {
            html: headingHtml,
            isPageHeading: true,
            classes: "govuk-fieldset__legend--l"
          }
        },
        items: locations
      }) }}

      {{ govukButton({
        text: "Continue"
      }) }}

    </form>

    <p class="govuk-body">
      <a class="govuk-link" href="{{ actions.cancel }}">Cancel</a>
    </p>

  </div>
</div>
{% endblock %}
