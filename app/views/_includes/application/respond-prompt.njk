{% if application.status == 'Awaiting decision' or application.status == 'Awaiting interview' %}
  {% set promptHtml %}
    <h2 class="govuk-heading-m govuk-!-margin-bottom-3">Set up an interview or make a decision</h2>
    <p>You have 22 days to make a decision – this application will be automatically rejected on 19 October 2020.</p>
    {{ govukButton({
      text: "Respond to application",
      classes: "govuk-!-margin-bottom-",
      href: "/application/" + application.id + "/decision"
    }) }}

    {# <p>Or </p>

    <a href="/application/{{application.id}}/decision">Make a decision</a> #}

    {# <p><a href="/application/{{application.id}}/interviews/new">Set up an interview</a> or <a href="/application/{{application.id}}/decision">make a decision</a></p> #}

  {% endset %}
  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">
      {{govukInsetText({
        html: promptHtml,
        classes: 'govuk-!-margin-top-0'
      })}}
    </div>
  </div>

{% endif %}
