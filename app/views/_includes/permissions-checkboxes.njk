{% if not fieldName %}
  {% set fieldName = 'data.orgpermissions.a' + relationship.id%}
{% endif %}

{{ govukCheckboxes({
  fieldset: {
    legend: {
      text: "Who can make offers and reject applications?",
      classes: "govuk-fieldset__legend--m"
    }
  },
  items: [
    {
      value: org,
      text: org
    },
    {
      value: partner,
      text: partner
    }
  ]
} | decorateAttributes(data, fieldName+'.makeDecisions')) }}

{{ govukCheckboxes({
  fieldset: {
    legend: {
      text: "Who can view criminal convictions and professional misconduct?",
      classes: "govuk-fieldset__legend--m"
    }
  },
  items: [
    {
      value: org,
      text: org
    },
    {
      value: partner,
      text: partner
    }
  ]
} | decorateAttributes(data, fieldName+'.viewSafeguardingInformation')) }}

{{ govukCheckboxes({
  fieldset: {
    legend: {
      text: "Who can view sex, disability and ethnicity information?",
      classes: "govuk-fieldset__legend--m"
    }
  },
  items: [
    {
      value: org,
      text: org
    },
    {
      value: partner,
      text: partner
    }
  ]
} | decorateAttributes(data, fieldName+'.viewDiversityInformation')) }}
