{% if statusText == 'Interviewing' %}
  {% set promptHtml %}
    <h2 class="govuk-heading-m govuk-!-margin-bottom-3">Make a decision</h2>
    {% if application.daysToRespond == 0 %}
      <p>You have less than a day to make a decision - this application will be automatically rejected on {{application.respondByDate | govukDate }}.</p>
    {% else %}
      <p>You have {{application.daysToRespond}} days to make a decision – this application will be automatically rejected on {{application.respondByDate | govukDate }}.</p>
    {% endif %}
    {{ govukButton({
      text: "Make decision",
      classes: "govuk-!-margin-bottom-0 ",
      href: "/applications/" + application.id + "/decision"
    }) }}
  {% endset %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-two-thirds-from-desktop">
      {{govukInsetText({
        html: promptHtml,
        classes: 'govuk-!-margin-top-0'
      })}}
    </div>
  </div>
{% endif %}


